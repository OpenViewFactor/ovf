#ifndef OVF_TESTGLOBALS_H_
#define OVF_TESTGLOBALS_H_

#include <string>
#include <vector>

// Input and Output directory for testing
inline std::string OVF_INPUT(const std::string &path) {
    return (std::string("${TESTS_INPUT_DIR}/") + path);
}
inline std::string OVF_OUTPUT(const std::string &path) {
    return (std::string("${TESTS_OUTPUT_DIR}/") + path);
}

#endif